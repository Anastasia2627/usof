import {Router} from 'express';import * as p from '../controllers/postsController.js';import {listPostComments,createComment} from '../controllers/commentsController.js';import {optionalAuth,requireAuth} from '../middleware/auth.js';
const r=Router();r.get('/',optionalAuth,p.listPosts);r.get('/:post_id',optionalAuth,p.getPost);r.get('/:post_id/comments',optionalAuth,listPostComments);r.post('/:post_id/comments',requireAuth,createComment);r.get('/:post_id/categories',optionalAuth,p.getPostCategories);r.get('/:post_id/like',optionalAuth,p.getPostReactions);r.post('/',requireAuth,p.createPost);r.post('/:post_id/like',requireAuth,p.reactToPost);r.patch('/:post_id',requireAuth,p.updatePost);r.delete('/:post_id',requireAuth,p.deletePost);r.delete('/:post_id/like',requireAuth,p.removePostReaction);export default r;
